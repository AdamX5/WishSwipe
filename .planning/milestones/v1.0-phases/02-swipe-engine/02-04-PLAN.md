---
phase: 02-swipe-engine
plan: 04
type: execute
wave: 4
depends_on: [02-03]
files_modified: []
autonomous: false
requirements: [SWIPE-01, SWIPE-02, SWIPE-03, SWIPE-04, SWIPE-05, GHOST-01, GHOST-02, UX-01, UX-02, UX-03]

must_haves:
  truths:
    - "User can open /swipe and see exactly 3 product cards stacked in the viewport"
    - "Dragging a card left or right tracks 1:1 with the pointer with no perceived lag"
    - "A fast flick or long drag slings the card off screen; a slow short drag snaps back"
    - "Right-swipe saves the product and a wishlist record appears in the Convex dashboard"
    - "Left-swipe does not create a wishlist record — only a swipe record with direction='left'"
    - "Tapping Undo restores the last swiped card to the top of the deck"
    - "Green overlay with heart icon appears during rightward drag; red overlay with X during leftward drag"
    - "Green glow ring pulses on the card for ~600ms after a confirmed right-swipe"
  artifacts:
    - path: "app/swipe/_components/SwipeDeck.tsx"
      provides: "Complete gesture engine"
    - path: "app/swipe/_components/SwipeCard.tsx"
      provides: "Card with glow and overlay"
    - path: "app/swipe/_components/SwipeOverlay.tsx"
      provides: "Directional tint overlay"
    - path: "convex/swipes.ts"
      provides: "recordSwipe and undoSwipe mutations"
    - path: "convex/cardQueue.ts"
      provides: "getCardQueue query"
  key_links:
    - from: "user drag gesture"
      to: "Convex swipes table"
      via: "recordSwipe mutation on gesture completion"
      pattern: "recordSwipeMutation"
---

<objective>
Human verification of all Phase 2 success criteria. Confirm the full swipe engine works end-to-end: gesture feel, card DOM invariant, Convex writes, undo, and all micro-UX feedback.

Purpose: No automated test can verify 1:1 gesture tracking, spring physics feel, or haptic feedback — human confirmation is required before marking Phase 2 complete.
Output: All 5 roadmap success criteria confirmed; phase marked complete.
</objective>

<execution_context>
@/Users/adam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-swipe-engine/02-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Full Phase 2 end-to-end verification</name>
  <action>
    Present the following verification checklist to the user. Wait for their response before continuing.

    Setup required before verifying:
    1. `npx convex dev` running in a terminal (or Convex deployed)
    2. Products table populated — run `npx convex run normaliser/actions:ingestAllStores` if empty
    3. `npm run dev` running and user signed into http://localhost:3000/swipe
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -5</automated>
  </verify>
  <done>User types "approved" confirming all 8 manual checks pass.</done>
  <what-built>
    Complete Phase 2 Swipe Engine:
    - 3-card gesture deck (SwipeDeck.tsx) using useSprings + useDrag
    - Product cards from Convex getCardQueue query
    - recordSwipe mutation (right → wishlist + swipe, left → swipe only)
    - undoSwipe mutation + Undo button
    - SwipeOverlay (green/red tint + heart/X icons during drag)
    - Glow pulse animation on right-swipe (animate-glow-pulse)
    - Haptic feedback on right-swipe (navigator.vibrate(50))
  </what-built>
  <how-to-verify>
    **Check 1 — SWIPE-01: 3 cards in DOM**
    - Open DevTools → Elements
    - Locate the SwipeDeck container
    - Count the `animated.div` card wrappers — expect exactly 3 (or fewer if queue has < 3 products)
    - Verify the topmost card is interactive (draggable)

    **Check 2 — SWIPE-02: 1:1 tracking**
    - Click and hold on the top card, drag slowly left and right
    - The card must follow the pointer with NO perceived lag
    - If the card lags or jumps, this check fails

    **Check 3 — SWIPE-03: Fling out vs snap back**
    - Slowly drag the card a short distance (< 100px) and release — card must spring back to center
    - Quickly flick the card past 100px or with fast velocity — card must fly off screen
    - After flying off, a new card should become the active top card

    **Check 4 — SWIPE-04: Convex writes (right and left)**
    - Right-swipe one card: open Convex Dashboard → Data → swipes table — verify direction='right' + all snapshot fields
    - Open Convex Dashboard → Data → wishlists table — verify a wishlist record exists for the same product
    - Left-swipe another card: verify swipes table gets direction='left' record; wishlists table did NOT get a new entry

    **Check 5 — SWIPE-05: Undo**
    - Swipe a card (right or left), then click the Undo button
    - Verify the card re-appears at the top of the deck
    - Open Convex Dashboard → swipes table — verify the swipe record was deleted
    - If undone swipe was right: verify the wishlist entry was also deleted

    **Check 6 — UX-03: Directional overlay**
    - Drag the top card slowly to the right — green tint with heart icon appears and intensifies
    - Drag slowly to the left — red tint with X icon appears and intensifies
    - Overlay fades when card snaps back to center

    **Check 7 — UX-02: Glow animation**
    - Complete a right-swipe and immediately observe the card — green glow ring pulses and fades over ~600ms

    **Check 8 — UX-01: Haptic (mobile only)**
    - On an Android device with Chrome, right-swipe a card — expect a brief ~50ms vibration pulse
    - Silently skipped on iOS and desktop — not a failure if not felt on non-Android
  </how-to-verify>
  <resume-signal>
    Type "approved" if all checks pass.
    Or describe which checks failed and what behavior was observed — Claude will diagnose and fix.
  </resume-signal>
</task>

</tasks>

<verification>
All 5 Phase 2 roadmap success criteria met:
1. Exactly 3 cards in DOM (or empty state when queue exhausted)
2. 1:1 card tracking during drag — no lag visible
3. Fling-out vs snap-back spring physics working correctly
4. Swipe direction correctly recorded; right-swipe creates wishlist entry
5. Undo restores card and reverts Convex records
</verification>

<success_criteria>
Human confirms all 8 verification checks pass. Phase 2 can be marked complete in ROADMAP.md and STATE.md.
</success_criteria>

<output>
After approval, create `.planning/phases/02-swipe-engine/02-04-SUMMARY.md` and update ROADMAP.md Phase 2 status to complete.
</output>
