---
phase: 03-wishlist
plan: 03
type: execute
wave: 1
depends_on:
  - 03-01
files_modified:
  - components/BottomNav.tsx
  - app/wishlist/_components/WishlistCard.tsx
  - app/wishlist/_components/WishlistSheet.tsx
autonomous: true
requirements:
  - WISH-01
  - WISH-02
must_haves:
  truths:
    - "BottomNav highlights the active tab (Discover vs Wishlist) based on current pathname"
    - "WishlistCard renders product image, title, and price (not star rating or store — those are sheet-only)"
    - "WishlistSheet slides up from bottom on open, shows large image + title + price + store + Visit Store + Remove"
    - "Visit Store opens affiliateUrl in a new tab — never navigates away from WishSwipe"
    - "Remove button calls onRemove then onClose immediately — no confirmation dialog"
  artifacts:
    - path: "components/BottomNav.tsx"
      provides: "Persistent bottom nav: Discover (/swipe) + Wishlist (/wishlist) tabs, active state via usePathname"
    - path: "app/wishlist/_components/WishlistCard.tsx"
      provides: "Presentational grid card: image, title, formatted price; calls onOpen on click"
    - path: "app/wishlist/_components/WishlistSheet.tsx"
      provides: "Bottom sheet: CSS transition translate-y, affiliate link, remove button, Escape key + backdrop close"
  key_links:
    - from: "app/wishlist/_components/WishlistSheet.tsx"
      to: "productSnapshot.affiliateUrl"
      via: "<a href={affiliateUrl} target='_blank' rel='noopener noreferrer'>"
      pattern: "affiliateUrl.*_blank"
    - from: "components/BottomNav.tsx"
      to: "usePathname"
      via: "pathname.startsWith('/swipe') / pathname.startsWith('/wishlist')"
      pattern: "usePathname"
---

<objective>
Build the three presentational components needed for the wishlist UI: the shared BottomNav (two-tab persistent nav), WishlistCard (image-forward grid cell), and WishlistSheet (bottom sheet detail with affiliate link and remove). All three are prop-driven and contain no Convex calls.

Purpose: Plan 03-04 (page wiring) imports these directly. Building them separately and in parallel with Plan 03-02 (Convex backend) halves wall-clock execution time.
Output: Three component files, each with clear prop interfaces. No Convex SDK imports in any of them.
</objective>

<execution_context>
@/Users/adam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-wishlist/03-RESEARCH.md

<interfaces>
<!-- productSnapshot shape — from convex/schema.ts wishlists table -->
```typescript
type ProductSnapshot = {
  title: string
  imageUrl: string
  priceAmount: number       // integer cents — divide by 100 for display
  priceCurrency: string     // "USD"
  affiliateUrl: string      // ONLY URL field — never rawUrl/productUrl/storeUrl
  sourceStore: string
}

// Full wishlist entry (what WishlistShell.tsx will pass down)
type WishlistItem = {
  _id: string               // Id<'wishlists'>
  userId: string
  productId: string
  savedAt: number
  productSnapshot: ProductSnapshot
}
```

<!-- Price formatter — use Intl.NumberFormat with explicit locale, not system default -->
```typescript
function formatPrice(cents: number, currency: string): string {
  return new Intl.NumberFormat('en-US', { style: 'currency', currency }).format(cents / 100)
}
// 999 → "$9.99",  12999 → "$129.99",  0 → "$0.00"
```

<!-- Established styling patterns — Tailwind CSS v4, no config file -->
// Bottom sheet: fixed, bottom-0, rounded-t-2xl, z-50, transition-transform duration-300
// translate-y-0 when open, translate-y-full when closed
// Backdrop: fixed inset-0 bg-black/40, z-40, opacity-100/opacity-0 + pointer-events-none toggle

<!-- BottomNav uses 'use client' for usePathname -->
import { usePathname } from 'next/navigation'
import Link from 'next/link'
// Tab active: text-black font-semibold | Tab inactive: text-gray-400
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BottomNav component</name>
  <files>components/BottomNav.tsx</files>
  <action>
Create `components/BottomNav.tsx` as a client component (requires `'use client'` for usePathname).

Props: none — reads current route internally.

Structure:
- `<nav>` fixed bottom-0, left-0, right-0, bg-white border-t border-gray-200, flex, z-30
- Two `<Link>` tabs: one to `/swipe`, one to `/wishlist`
- Each tab: flex-1, flex flex-col items-center, py-3, gap-1, text-xs
- Active state detected via `usePathname()` with `pathname.startsWith('/swipe')` and `pathname.startsWith('/wishlist')`
- Active tab classes: `text-black font-semibold` | Inactive: `text-gray-400`
- Discover tab (left): icon `◎` (or similar compass/grid unicode), label "Discover"
- Wishlist tab (right): icon `♡` (heart unicode), label "Wishlist"

At Claude's discretion: exact icon, icon size (text-xl assumed), active indicator (underline, dot, or weight change).

Do NOT add logic for fetching data. Purely presentational + routing.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>File exists. `'use client'` directive present. Both Link hrefs correct (/swipe, /wishlist). Active state uses usePathname. npm test passes.</done>
</task>

<task type="auto">
  <name>Task 2: WishlistCard + WishlistSheet components</name>
  <files>app/wishlist/_components/WishlistCard.tsx, app/wishlist/_components/WishlistSheet.tsx</files>
  <action>
Create both components in `app/wishlist/_components/` (create the directory).

**WishlistCard.tsx** — presentational grid cell, NOT `'use client'` required (no hooks):
Props:
```typescript
type Props = {
  item: WishlistItem
  onOpen: (item: WishlistItem) => void
}
```
Render:
- `<button>` (not `<a>`) — calls `onOpen(item)` on click
- Product image: `<img src={item.productSnapshot.imageUrl} alt={item.productSnapshot.title} className="w-full aspect-square object-cover rounded-xl" />`
- Title: truncated to 2 lines (`line-clamp-2`)
- Price: `formatPrice(item.productSnapshot.priceAmount, item.productSnapshot.priceCurrency)` — include formatPrice as a local utility in this file
- Do NOT show star rating or store name on the grid card (those belong in the sheet only, per user decision)

**WishlistSheet.tsx** — bottom sheet, `'use client'` required (uses useEffect for Escape key):
Props:
```typescript
type Props = {
  item: WishlistItem | null
  onClose: () => void
  onRemove: (wishlistId: string) => void
}
```
Render:
- Backdrop div: `fixed inset-0 bg-black/40 transition-opacity z-40`, opacity-100 when open / `opacity-0 pointer-events-none` when closed, onClick → onClose
- Sheet div: `fixed bottom-0 left-0 right-0 bg-white rounded-t-2xl z-50 p-6 transition-transform duration-300`, `translate-y-0` when open / `translate-y-full` when closed
- `isOpen = item !== null`
- Inside sheet (only rendered when item is not null): drag handle, large image (w-full h-48 object-cover rounded-xl), title (font-semibold text-lg), sourceStore (text-gray-500), formatted price (font-bold text-xl)
- Escape key: `useEffect` adds/removes `keydown` listener, calls onClose on Escape
- Two buttons in a flex row:
  1. "Visit Store" — `<a href={item.productSnapshot.affiliateUrl} target="_blank" rel="noopener noreferrer">` — styled as primary CTA (bg-black text-white rounded-full py-3 flex-1 text-center font-medium). NEVER use window.location.href or window.open — use anchor tag with target="_blank"
  2. "Remove" — `<button onClick={() => { onRemove(item._id); onClose() }}>` — styled as secondary (border border-gray-200 rounded-full px-6 py-3 text-gray-700 font-medium). No confirmation dialog — instant action per user decision.

Include `formatPrice` helper in WishlistSheet as well (or import from a shared utils file if you prefer — at Claude's discretion, but do NOT create a new package).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Both files exist. WishlistCard shows image/title/price only (not rating/store). WishlistSheet has affiliate anchor with target="_blank" rel="noopener noreferrer". Remove calls onRemove then onClose with no dialog. npm test passes.</done>
</task>

</tasks>

<verification>
`npm test` green.
`components/BottomNav.tsx` exists with usePathname active-state logic.
`app/wishlist/_components/WishlistCard.tsx` exists, no star rating or store name.
`app/wishlist/_components/WishlistSheet.tsx` exists, affiliateUrl used in `<a target="_blank">`, Escape key handler present, Remove has no confirm dialog.
</verification>

<success_criteria>
Three components committed. Plan 03-04 can import them without modification. No Convex SDK imports in any component.
</success_criteria>

<output>
After completion, create `.planning/phases/03-wishlist/03-03-SUMMARY.md`
</output>
