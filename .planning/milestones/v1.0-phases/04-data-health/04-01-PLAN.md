---
phase: 04-data-health
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - convex/schema.ts
  - convex/normaliser/__tests__/compaction.test.ts
autonomous: true
requirements:
  - GHOST-03

must_haves:
  truths:
    - "Pure filter logic `isCompactable` returns true only for left-swipes older than the cutoff"
    - "Pure filter logic returns false for right-swipes of any age (safety invariant)"
    - "Pure filter logic returns false for left-swipes newer than the cutoff"
    - "Schema has `by_direction_time` index enabling efficient direction+age queries on swipes table"
  artifacts:
    - path: "convex/normaliser/__tests__/compaction.test.ts"
      provides: "Failing unit tests for isCompactable and filterCompactable pure functions"
      min_lines: 60
    - path: "convex/schema.ts"
      provides: "by_direction_time index on swipes table"
      contains: "by_direction_time"
  key_links:
    - from: "convex/normaliser/__tests__/compaction.test.ts"
      to: "convex/compaction.ts"
      via: "pure function mirror pattern — tests define contract, implementation satisfies it in Plan 04-02"
      pattern: "isCompactable|filterCompactable"
---

<objective>
Wave 0 TDD scaffold for the compaction logic: write failing pure-function tests that specify the filter predicate's exact behavior, then add the `by_direction_time` schema index that makes compaction queries efficient.

Purpose: Establish the behavioral contract before implementation, matching the Wave 0 pattern used in Phase 2 (swipes.test.ts) and Phase 3 (wishlists.test.ts). Tests run RED here; Plan 04-02 turns them GREEN.

Output: Failing test file with 6 test cases, updated schema with additive index.
</objective>

<execution_context>
@/Users/adam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-data-health/04-RESEARCH.md
@.planning/phases/04-data-health/04-VALIDATION.md
</context>

<feature>
  <name>Compaction filter logic — Wave 0 test scaffold</name>
  <files>convex/normaliser/__tests__/compaction.test.ts, convex/schema.ts</files>
  <behavior>
    isCompactable(swipe, cutoffMs):
    - left-swipe older than cutoff (swipedAt < cutoffMs) → true
    - right-swipe older than cutoff → false (must NEVER be compacted)
    - left-swipe newer than cutoff (swipedAt >= cutoffMs) → false
    - right-swipe newer than cutoff → false

    filterCompactable(swipes, cutoffMs):
    - excludes all right-swipes regardless of age
    - excludes left-swipes newer than cutoff
    - returns empty array when no records exceed threshold
    - returns only the compactable subset when mixed records provided
  </behavior>
  <implementation>
    Task 1: Add `by_direction_time` index to schema.ts.

    Task 2: Create compaction.test.ts with failing tests. Define inline pure functions
    (same mirror pattern as wishlists.test.ts) and write 6 test cases that specify the
    compaction filter predicate. No Convex SDK imports — tests must run in Jest with
    `npm test -- --testPathPattern=compaction` and exit RED (the functions are not yet
    implemented in compaction.ts).
  </implementation>
</feature>

<tasks>

<task type="auto">
  <name>Task 1: Add by_direction_time index to schema</name>
  <files>convex/schema.ts</files>
  <action>
    Add one additive index to the `swipes` table definition in `convex/schema.ts`. This is a schema-only change — no data migration needed, no existing indexes removed.

    Add after the existing `.index('by_user_product', ['userId', 'productId'])` line:
    ```typescript
    .index('by_direction_time', ['direction', 'swipedAt'])
    ```

    This compound index enables the compaction query to filter `direction='left'` and then range on `swipedAt < cutoff` at the index level — avoiding a full-table scan. The index is purely additive and does not affect any existing queries.

    Do NOT modify any other table, field, or index in schema.ts.
  </action>
  <verify>
    <automated>grep -n "by_direction_time" /Users/adam/Documents/01\ Projects/Coding/WishSwipe/convex/schema.ts</automated>
  </verify>
  <done>schema.ts contains `.index('by_direction_time', ['direction', 'swipedAt'])` on the swipes table and the file is syntactically valid TypeScript</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write failing compaction.test.ts — pure function scaffold</name>
  <files>convex/normaliser/__tests__/compaction.test.ts</files>
  <behavior>
    - isCompactable: left older than cutoff → true
    - isCompactable: right older than cutoff → false
    - isCompactable: left newer than cutoff → false
    - isCompactable: right newer than cutoff → false
    - filterCompactable: excludes all right-swipes regardless of age
    - filterCompactable: returns empty array when no records exceed threshold
  </behavior>
  <action>
    Create `convex/normaliser/__tests__/compaction.test.ts` following the exact mirror pattern from `convex/normaliser/__tests__/wishlists.test.ts`:

    1. Define the `SwipeRecord` type inline (no Convex SDK imports):
    ```typescript
    type SwipeRecord = {
      _id: string
      userId: string
      direction: 'right' | 'left'
      swipedAt: number
    }
    ```

    2. Define the pure functions inline as the behavioral contract (these are stubs that mirror what compaction.ts will export):
    ```typescript
    function isCompactable(swipe: SwipeRecord, cutoffMs: number): boolean {
      // STUB — Wave 0 tests run RED; Plan 04-02 implements this in convex/compaction.ts
      throw new Error('not implemented')
    }

    function filterCompactable(swipes: SwipeRecord[], cutoffMs: number): SwipeRecord[] {
      return swipes.filter(s => isCompactable(s, cutoffMs))
    }
    ```

    3. Write 6 test cases in 2 describe blocks:

    `describe('isCompactable')`:
    - `returns true for left-swipe older than cutoff` — direction: 'left', swipedAt below cutoff → expect true
    - `returns false for right-swipe older than cutoff` — direction: 'right', swipedAt below cutoff → expect false (safety invariant)
    - `returns false for left-swipe newer than cutoff` — direction: 'left', swipedAt above cutoff → expect false
    - `returns false for right-swipe newer than cutoff` — direction: 'right', swipedAt above cutoff → expect false

    `describe('filterCompactable')`:
    - `excludes all right-swipes regardless of age` — mix of right-swipes older and newer than cutoff, all left-swipes newer than cutoff → result should be empty
    - `returns empty array when no records exceed threshold` — all swipes newer than cutoff → result.length === 0

    Use a fixed `CUTOFF = 1_000_000` ms and explicit `swipedAt` values (e.g., 500_000 for "old", 2_000_000 for "new") — no `Date.now()` calls in tests.

    Zero Convex SDK imports. File must be runnable with: `npm test -- --testPathPattern=compaction`

    Tests MUST run RED (the stubs throw). This is intentional — Wave 0 RED is the correct state.
  </action>
  <verify>
    <automated>cd /Users/adam/Documents/01\ Projects/Coding/WishSwipe && npm test -- --testPathPattern=compaction 2>&1 | tail -20</automated>
  </verify>
  <done>compaction.test.ts exists with 6 tests across 2 describe blocks, zero Convex SDK imports. Tests run and fail with "not implemented" error (RED state confirmed). `npm test` full suite still passes all non-compaction tests.</done>
</task>

</tasks>

<verification>
- `grep -n "by_direction_time" convex/schema.ts` returns a match
- `npm test -- --testPathPattern=compaction` runs 6 tests, all RED (not skipped, not erroring on import)
- `npm test` full suite: all non-compaction tests remain green
</verification>

<success_criteria>
- schema.ts has `by_direction_time` index on swipes table (additive, no other changes)
- compaction.test.ts has 6 failing tests defining the isCompactable + filterCompactable behavioral contract
- Zero Convex SDK imports in the test file
- Wave 0 RED state confirmed — ready for Plan 04-02 GREEN implementation
</success_criteria>

<output>
After completion, create `.planning/phases/04-data-health/04-01-SUMMARY.md`
</output>
