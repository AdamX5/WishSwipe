---
phase: 03-wishlist
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - convex/normaliser/__tests__/wishlists.test.ts
autonomous: true
requirements:
  - WISH-01
  - WISH-02
must_haves:
  truths:
    - "Pure filter logic for getWishlist (filter by userId) is tested and documented before implementation"
    - "Pure ownership-check logic for removeFromWishlist (entry.userId === user._id guard) is tested before implementation"
  artifacts:
    - path: "convex/normaliser/__tests__/wishlists.test.ts"
      provides: "Wave 0 test scaffold — pure function unit tests for wishlist query filter and delete ownership check"
      min_lines: 40
  key_links:
    - from: "convex/normaliser/__tests__/wishlists.test.ts"
      to: "convex/wishlists.ts"
      via: "Pure function mirrors — tests exercise the same logic as the Convex handlers"
      pattern: "filterWishlistByUser|checkOwnership"
---

<objective>
Create the Wave 0 test scaffold for Phase 3. Defines pure helper functions and tests covering the two core behaviors — filtering wishlists by user and enforcing ownership before delete — exactly as established by cardQueue.test.ts and swipes.test.ts.

Purpose: Nyquist compliance requires tests exist before implementation. These tests will turn GREEN when Plan 03-02 implements the Convex backend functions.
Output: `convex/normaliser/__tests__/wishlists.test.ts` with all tests passing (pure functions, no Convex SDK).
</objective>

<execution_context>
@/Users/adam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-wishlist/03-VALIDATION.md

<interfaces>
<!-- Established test patterns — copy structure exactly -->
<!-- From convex/normaliser/__tests__/cardQueue.test.ts -->
```typescript
// Pure helper function — no Convex SDK imports
function filterUnswiped(products, swipedIds, limit = 20): Product[]

// describe blocks with it() assertions
describe('filterUnswiped', () => {
  it('excludes products that have been swiped', ...)
  it('returns empty array when all products are swiped', ...)
})
```

<!-- From convex/normaliser/__tests__/swipes.test.ts -->
```typescript
// Pure helper function — no Convex SDK imports
function validateSnapshotShape(snapshot: unknown): boolean
function validateDirection(direction: unknown): boolean
```

<!-- From convex/schema.ts — wishlists table shape -->
type WishlistEntry = {
  _id: string
  userId: string
  productId: string
  savedAt: number
  productSnapshot: {
    title: string
    imageUrl: string
    priceAmount: number    // integer cents
    priceCurrency: string
    affiliateUrl: string   // ONLY URL field — no rawUrl/productUrl/storeUrl
    sourceStore: string
  }
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write wishlists.test.ts — pure function test scaffold</name>
  <files>convex/normaliser/__tests__/wishlists.test.ts</files>
  <action>
Create `convex/normaliser/__tests__/wishlists.test.ts` following the exact pattern of cardQueue.test.ts and swipes.test.ts — pure functions, zero Convex SDK imports.

Define two pure helper functions inline in the test file (same pattern as cardQueue.test.ts defines filterUnswiped locally):

**Function 1: filterWishlistByUser**
```typescript
function filterWishlistByUser(
  entries: Array<WishlistEntry>,
  userId: string
): Array<WishlistEntry> {
  return entries.filter(e => e.userId === userId)
}
```

**Function 2: checkOwnership**
```typescript
function checkOwnership(entry: WishlistEntry, userId: string): boolean {
  return entry.userId === userId
}
```

**Function 3: formatPrice**
```typescript
function formatPrice(cents: number, currency: string): string {
  return new Intl.NumberFormat('en-US', { style: 'currency', currency }).format(cents / 100)
}
```

Write describe blocks and it() tests covering:

`describe('filterWishlistByUser')`:
- Returns only entries matching userId
- Returns empty array when no entries match userId
- Returns empty array when entries list is empty
- Excludes entries from other users

`describe('checkOwnership')`:
- Returns true when entry.userId matches userId
- Returns false when entry.userId does not match (different user — prevents delete of another user's entry)
- Returns false with empty string userId

`describe('formatPrice')`:
- Formats 999 cents as "$9.99"
- Formats 12999 cents as "$129.99"
- Formats 0 cents as "$0.00"

Use a `makeEntry` helper to create test WishlistEntry objects (same as swipes.test.ts uses `validSnapshot`).
Do NOT import from convex SDK — these are pure function tests only.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=wishlists</automated>
  </verify>
  <done>All tests pass. `npm test` exits with code 0. Test file covers both WISH-01 filter logic and WISH-02 ownership/remove logic as pure functions.</done>
</task>

</tasks>

<verification>
`npm test -- --testPathPattern=wishlists` exits 0 with all tests green.
No Convex SDK imports in the test file.
Test count: at least 9 it() blocks across 3 describe blocks.
</verification>

<success_criteria>
Wave 0 test scaffold committed. `npm test` green. Executor for Plan 03-02 has a clear behavioral contract for the Convex functions it will implement.
</success_criteria>

<output>
After completion, create `.planning/phases/03-wishlist/03-01-SUMMARY.md`
</output>
